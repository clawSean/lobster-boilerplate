# Runtime-only env vars for a separate second gateway (template; no real secrets)
# For the standard gateway, prefer docs/core/1password-runtime-secrets.md and
# openclaw-gateway.service.
OPENCLAW_GATEWAY_TOKEN=
TELEGRAM_BOT_TOKEN=
OPENAI_API_KEY=
BRAVE_API_KEY=
